# pseudomode CMake configuration file
@PACKAGE_INIT@

# Find dependencies
include(CMakeFindDependencyMacro)

# Optional: Find Eigen3
find_dependency(Eigen3 QUIET)

# Optional: Find OpenMP
find_dependency(OpenMP QUIET)

# Include targets
include("${CMAKE_CURRENT_LIST_DIR}/pseudomode-targets.cmake")

check_required_components(pseudomode)
